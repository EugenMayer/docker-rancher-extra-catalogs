version: '2'
services:
  v1:
    image: eugenmayer/volume-persistence-test:0.0.1
    build:
      context: .
      args:
        ARGVERSION: '0.0.1'
  v2:
    image: eugenmayer/volume-persistence-test:0.0.2
    build:
      context: .
      args:
        ARGVERSION: '0.0.2'
  v3:
    image: eugenmayer/volume-persistence-test:0.0.3
    build:
      context: .
      args:
        ARGVERSION: '0.0.3'
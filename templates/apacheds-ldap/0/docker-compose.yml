apacheds:
  image: eugenmayer/apacheds:m21-beta1
  environment:
    DS_KEYSTORE_PATH: "/bootstrap/apacheds.keystore"
  volumes:
    - ${STORAGEPATH}:/data
    - ${BOOTSTRAPPATH}:/bootstrap
  labels:
    io.rancher.container.create_agent: 'true'
    io.rancher.container.agent.role: 'environment'
